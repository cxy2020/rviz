# The latitude spacing between two paths.
path_latitude_spacing: 0.6
# The spacing bwtween the end of the path and the rectangle zone.
path_longitude_spacing: 0.5
# The max ditance error of the cleaning path.
max_distance_error: 0.05
# The max angle error of the cleaning path.
max_angle_error: 0.05
# The file to save the generated path.
# The max linear speed.
max_linear_speed: 1.5
# The max rotation speed.
max_rotation_speed: 1.0
default_file_dir: time_log
# The width of of the squre rectangle to relocate from amcl.
relocat_width: 1.0
